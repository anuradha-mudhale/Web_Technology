databinding is a binding techniques it connects UI to code
there are 2 types->
1 way ->interpolation,property binding,event binding
1]interpolation->{{value}}
2]property->{prop}=value
3]event-(click)=>()
4][(ngModel)] = 'value'

interpolation->show data from ts to html
bind html property to ts variable 
event->call function on event
ng model -> see data both ways->ts to html
html to ts
			
Modules-->before angular 15 we need ng moules now days we are using angular 21 version so modules are optional you can now use standalone components

directives
think the directives like magic wands that can change your web page without rewriting it there are two types are
1]structural->change the structure of your page like add,remove or repeated things -->ngif,nFor,ngswitch
2]attribute directives->ng style

HomeActivity
1]how we can use if else for loop switch case write with an example

Routing->
navigation between one page to another page is called routing 

---------------Activity-----------------------

Hide div remove show btn div display
toggle
num1==num2 

Services:services is a like a helper class it is used to store a data and share logic so many components are use instead of writing same code in every components we write once in a service
command:ng 

ngOnInit is a lifecycle hook this method runs automatically when components run

ngOnInit(){
this.students = this.studentServices
}

routing  : navigation between one page to another page
step1:->there are 3 steps







	